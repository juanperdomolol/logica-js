var pulgadas = 39.37
var pies = 3.28084

function conversion(metros){
    var resPulgadas = metros * pulgadas
    var resPies = metros * pies
    console.log(`${metros} metros son ${resPulgadas} pulgas y ${resPies} pies`)
}